server:
  port: 8090

spring:
  application:
    name: gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: servicio-a
              uri: lb://servicio-a
              predicates:
                - Path=/api/**
              filters:
                - StripPrefix=1
								
					- id: servicio-b
							uri: lb://servicio-b
						  predicates:
							  - Path=/api/**
						  filters:
							  - StripPrefix=1
    loadbalancer:
      enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka